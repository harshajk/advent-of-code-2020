Lambdas:
# A lambda can have multiple arguments separated by commas
#   lambda arguments: expression
# the expression always returns an object

# lambda functions are required when function objects are required.


Setup related:
1. Installing python modules in vs code. Run the following commands:
    py -3 -m venv .venv
    .venv\scripts\activate

Misc:
taskkill /F /PID pid_number